# YONOD 数据集输入与合法性检验流程重构可行性分析报告

**生成时间**: 2026-06-28
**分析对象**: `YONOD数据集输入与合法性检验流程重构要求.md`
**分析版本**: v1.0

---

## 目录

1. [总体评估](#总体评估)
2. [可行性分析（按步骤）](#可行性分析按步骤)
3. [需求完整性检查](#需求完整性检查)
4. [技术难点与风险](#技术难点与风险)
5. [架构兼容性分析](#架构兼容性分析)
6. [改进建议](#改进建议)
7. [实施建议](#实施建议)

---

## 总体评估

### 可行性评级: ⭐⭐⭐⭐☆ (4/5)

### 总体结论

该重构需求**整体可行**，与现有代码架构兼容性良好。重构方向清晰，技术难度适中，但需要注意以下关键点：

**优势**:
- ✅ 需求与现有 wizard 交互流程高度契合（已有列选择、验证机制）
- ✅ 核心验证逻辑已在 `yonod.py` 中实现（`_validate_label`、`_validate_smiles`、`_normalize_smiles`）
- ✅ 描述符拼接逻辑已在 `universal/feature_builder.py` 中存在基础设施
- ✅ 现有代码已支持列角色分类（`reactant_cols`、`product_cols`、`other_cols`）

**风险**:
- ⚠️ 规范数据集生成逻辑需要新开发（现有代码无对应功能）
- ⚠️ 非法输入排除报告生成需要新开发
- ⚠️ 描述符嵌入配置的可交互编辑功能需要设计用户友好的界面

---

## 可行性分析（按步骤）

### 步骤 1: 指定初始数据集、项目名称和项目文件夹位置

**可行性**: ✅ 高
**技术难度**: ⭐☆☆☆☆
**现有实现**: 已完成 90%

**分析**:
- 现有 wizard 的步骤 1、5、6 已实现此功能
- `yonod.py` 第 778-800 行已有 CSV 路径输入与验证逻辑
- 项目名称输入（第 924-933 行）已验证合法性（仅英文、数字、下划线、连字符）
- 输出目录指定（第 936-941 行）已支持默认值和自定义路径

**建议**:
- 保持现有实现即可，无需修改
- 可考虑在步骤 1 中一次性收集这三项（减少交互轮次）

---

### 步骤 2: 逐列输入 SMILES 并声明 SMILES 的类型

#### 2.1 读取初始数据集并展示列序号和列名称

**可行性**: ✅ 高
**技术难度**: ⭐☆☆☆☆
**现有实现**: 已完成 100%

**分析**:
- `_show_columns()` 函数（第 570-577 行）已实现 Excel 风格列序号显示（A, B, ..., AA, AB, ...）
- 支持多字母列名解析（`_excel_to_col_index` 函数，第 561-567 行）

---

#### 2.2 声明标签列

**可行性**: ✅ 高
**技术难度**: ⭐⭐☆☆☆
**现有实现**: 已完成 80%

**分析**:
- ✅ **已有功能**:
  - 标签列选择（第 804-809 行）
  - 数值合法性检验（`_validate_label`，第 634-670 行）
  - JSON 数组格式支持（如 `[0.85]` 或 `["0.85"]`）
  - 非法值检测与行号提示

- ⚠️ **需要新增**:
  1. **标签名称自定义**（默认为 `yield`）
  2. **英文与符号名称校验**（排除中文字符）
  3. **非法行记录机制**（用于后续生成报告）
  4. **继续执行而非退出**（当前遇到非法值立即 `sys.exit(1)`）

**技术难点**:
- 需要重构 `_validate_label` 函数，改为收集非法行索引而非立即退出
- 需要维护全局非法行记录（可用 `dict[int, list[str]]` 存储：行号 → 非法原因列表）

**实现建议**:
```python
def _validate_label_with_collection(df, label_col, columns, invalid_rows_tracker):
    """验证标签列并收集非法行，不立即退出。

    Args:
        invalid_rows_tracker: dict[int, list[str]], 存储 {行号: [非法原因列表]}

    Returns:
        (valid_mask, invalid_indices): 有效行掩码 + 非法行索引列表
    """
    # 验证逻辑同原函数，但遇到非法值时:
    # 1. 记录到 invalid_rows_tracker[row_idx].append(f"标签列 '{label_col}': 非数值 '{bad_val}'")
    # 2. 继续检查下一行
    # 3. 返回有效行掩码而非退出程序
```

---

#### 2.3 声明反应物 SMILES 列

**可行性**: ✅ 高
**技术难度**: ⭐⭐⭐☆☆
**现有实现**: 已完成 70%

**分析**:
- ✅ **已有功能**:
  - 多列选择（`_ask_multi_cols`，第 608-626 行）
  - SMILES 验证（`_validate_smiles`，第 726-765 行）
  - 多组分 SMILES 拆分支持（`split_multi_smiles`，`descriptors/base.py` 第 21-63 行）
  - JSON 数组格式规范化（`_normalize_smiles`，第 673-723 行）

- ⚠️ **需要新增**:
  1. **标签名称自定义**（默认 `reactant`，支持多列时自动编号为 `reactant-1`, `reactant-2`）
  2. **英文与符号名称校验**
  3. **空值允许**（与现有逻辑一致）
  4. **非法行收集**（同 2.2）

**技术难点**:
- 需要在用户输入列之后立即询问标签名称（或使用默认值）
- 标签名称冲突检测（避免与已有列名或其他标签名重复）

---

#### 2.4 声明产物 SMILES 列

**可行性**: ✅ 高
**技术难度**: ⭐⭐⭐⭐☆
**现有实现**: 已完成 60%

**分析**:
- ✅ **已有功能**:
  - 单列选择（`_ask_single_col`，第 596-605 行）
  - SMILES 验证（同 2.3）

- ⚠️ **需要新增**:
  1. **单元格单 SMILES 强制校验**（这是产物列独有的限制）
  2. 标签名称自定义（默认 `product`）
  3. 英文与符号名称校验
  4. 非法行收集

**技术难点**:
- **关键点**: 验证每个单元格仅包含一个独立 SMILES（不能有 `.`, `,`, `;` 分隔符）
- 需要在 SMILES 验证前增加预检查逻辑

**实现建议**:
```python
def _validate_single_smiles(df, product_col, columns, invalid_rows_tracker):
    """验证产物列每个单元格仅包含单个 SMILES。"""
    for raw_idx, val in enumerate(df[product_col]):
        if pd.isna(val):
            continue
        normalized = _normalize_smiles(str(val))
        components = split_multi_smiles(normalized)
        if len(components) > 1:
            invalid_rows_tracker[raw_idx].append(
                f"产物列 '{product_col}': 包含多个 SMILES '{val}'，产物列仅允许单个 SMILES"
            )
```

---

#### 2.5 声明其他组分 SMILES 列

**可行性**: ✅ 高
**技术难度**: ⭐⭐⭐☆☆
**现有实现**: 已完成 70%

**分析**:
- ✅ **已有功能**:
  - 现有 wizard 已支持 `other_cols`（第 887-907 行）
  - 多次输入循环机制（通过 while 循环实现）

- ⚠️ **需要新增**:
  1. **每列单独命名**（而非统一归为 `other`）
  2. **允许空值**（需要在验证逻辑中跳过 `pd.isna()` 的值）
  3. **多次输入确认机制**（"是否继续输入其他组分？"）

**技术难点**:
- 需要维护 `other_cols_with_labels: dict[str, str]`（列名 → 自定义标签名）
- 验证逻辑需要区分"空值合法"与"非法 SMILES"

---

#### 2.6 声明辅助型数值

**可行性**: ✅ 高
**技术难度**: ⭐⭐☆☆☆
**现有实现**: 已完成 80%

**分析**:
- ✅ **已有功能**:
  - 数值列选择（第 913-921 行）
  - 多列输入支持

- ⚠️ **需要新增**:
  1. **每次仅输入单列**（需要用 `_ask_single_col` 替换 `_ask_multi_cols`）
  2. **每列单独命名**
  3. **空值允许**（需要在验证时跳过 `pd.isna()`）
  4. **非法值收集**（检测非数值类型）
  5. **多次输入确认机制**

**实现建议**:
```python
def _validate_numeric_with_collection(df, numeric_col, columns, invalid_rows_tracker):
    """验证数值列并收集非法行。"""
    numeric = pd.to_numeric(df[numeric_col], errors="coerce")
    bad_mask = numeric.isna() & df[numeric_col].notna()  # 非空但无法转数值
    for raw_idx in bad_mask[bad_mask].index:
        bad_val = df[numeric_col].iloc[raw_idx]
        invalid_rows_tracker[raw_idx].append(
            f"数值列 '{numeric_col}': 非数值 '{bad_val}'"
        )
```

---

### 步骤 3: 生成规范数据集与非法输入排除报告

#### 3.1 生成非法输入排除报告

**可行性**: ✅ 高
**技术难度**: ⭐⭐⭐☆☆
**现有实现**: 0%（需全新开发）

**分析**:
- **输入**: `invalid_rows_tracker: dict[int, list[str]]`（步骤 2 收集的非法行记录）
- **输出**: Markdown 文档，包含：
  1. 排除行统计（哪些行在哪些步骤被排除）
  2. 逐行表格展示（表头 + 被排除行，非法值加粗）

**技术难点**:
- Markdown 表格生成（需要处理单元格内容转义）
- 非法值定位与加粗（需要在表格渲染时标记）
- 文件保存位置（项目文件夹下，需要在步骤 1 确定）

**实现建议**:
```python
def generate_exclusion_report(invalid_rows_tracker, df, columns, project_dir):
    """生成非法输入排除报告 Markdown。

    Args:
        invalid_rows_tracker: {行号: [非法原因列表]}
        df: 原始 DataFrame
        columns: 原始列名列表
        project_dir: 项目文件夹路径

    Returns:
        report_path: 生成的 Markdown 文件路径
    """
    # 1. 生成排除统计（按步骤分组）
    # 2. 逐行生成表格（使用 pandas to_markdown 或手动拼接）
    # 3. 加粗非法值（用正则替换匹配的值为 **值**）
    # 4. 保存到 f"{project_dir}/非法输入排除报告.md"
```

**用户体验优化**:
- 报告生成后自动打印保存路径
- 询问用户是否浏览确认（打开文件或直接继续）

---

#### 3.2 生成规范数据集

**可行性**: ⭐⭐⭐⭐☆ 中高
**技术难度**: ⭐⭐⭐⭐☆
**现有实现**: 0%（需全新开发）

**分析**:
- **核心逻辑**:
  1. 过滤掉非法行
  2. 拆分多组分 SMILES（逗号、分号、点号 → 独立列）
  3. 按要求排列列顺序：反应物 → 其他组分 → 辅助数值 → 产物 → 标签

**技术难点**:

1. **动态列数计算**（最复杂）:
   - 需要遍历所有行，找到每类组分的最大数量
   - 例如：反应物列有 3 列，但某行包含 5 个反应物 SMILES，需要创建 5 个 `reactant-X` 列
   - 这会导致规范数据集列数 > 原始列数

2. **SMILES 拆分与填充**:
   - 使用 `split_multi_smiles()` 拆分每个单元格
   - 组分不足时填充空值（允许空值）

3. **列命名规则**:
   - 用户自定义标签名 + 编号（如 `solvent-1`, `solvent-2`）
   - 需要维护映射关系（原始列 → 规范列列表）

4. **数据完整性校验**:
   - 产物列必须每行仅一个 SMILES（已在 2.4 验证）
   - 标签列必须非空（已在 2.2 验证）

**实现建议**:
```python
def generate_normalized_dataset(df, invalid_rows_tracker, column_roles, project_dir):
    """生成规范数据集 CSV。

    Args:
        df: 原始 DataFrame
        invalid_rows_tracker: {行号: [非法原因列表]}
        column_roles: {
            'reactant': [(col_name, label_name), ...],
            'product': [(col_name, label_name)],
            'other': [(col_name, label_name), ...],
            'numeric': [(col_name, label_name), ...],
            'label': (col_name, label_name)
        }
        project_dir: 项目文件夹路径

    Returns:
        normalized_df: 规范化后的 DataFrame
        normalized_csv_path: 保存的 CSV 路径
    """
    # 步骤 1: 过滤掉非法行
    valid_indices = set(range(len(df))) - set(invalid_rows_tracker.keys())
    df_valid = df.iloc[list(valid_indices)].reset_index(drop=True)

    # 步骤 2: 计算每类组分的最大列数
    max_counts = {'reactant': 0, 'other': {}, 'numeric': {}}
    for idx, row in df_valid.iterrows():
        # 反应物
        reactant_total = 0
        for col, label in column_roles['reactant']:
            val = row[col]
            if pd.notna(val):
                components = split_multi_smiles(_normalize_smiles(str(val)))
                reactant_total += len(components)
        max_counts['reactant'] = max(max_counts['reactant'], reactant_total)

        # 其他组分（每个标签独立计数）
        for col, label in column_roles['other']:
            if label not in max_counts['other']:
                max_counts['other'][label] = 0
            val = row[col]
            if pd.notna(val):
                components = split_multi_smiles(_normalize_smiles(str(val)))
                max_counts['other'][label] = max(max_counts['other'][label], len(components))

        # 数值列（每个标签独立计数，通常为 1）
        for col, label in column_roles['numeric']:
            if label not in max_counts['numeric']:
                max_counts['numeric'][label] = 0
            if pd.notna(row[col]):
                max_counts['numeric'][label] = max(max_counts['numeric'][label], 1)

    # 步骤 3: 创建规范列名
    normalized_columns = []
    # 反应物
    for i in range(max_counts['reactant']):
        normalized_columns.append(f"reactant-{i+1}")
    # 其他组分
    for col, label in column_roles['other']:
        for i in range(max_counts['other'].get(label, 0)):
            normalized_columns.append(f"{label}-{i+1}")
    # 数值列
    for col, label in column_roles['numeric']:
        if max_counts['numeric'].get(label, 0) > 0:
            normalized_columns.append(label)
    # 产物
    normalized_columns.append(column_roles['product'][1])  # label_name
    # 标签
    normalized_columns.append(column_roles['label'][1])

    # 步骤 4: 填充数据
    normalized_data = []
    for idx, row in df_valid.iterrows():
        new_row = {}

        # 填充反应物（横向合并所有反应物列）
        reactant_smiles = []
        for col, label in column_roles['reactant']:
            val = row[col]
            if pd.notna(val):
                components = split_multi_smiles(_normalize_smiles(str(val)))
                reactant_smiles.extend(components)
        for i, smi in enumerate(reactant_smiles):
            new_row[f"reactant-{i+1}"] = smi
        for i in range(len(reactant_smiles), max_counts['reactant']):
            new_row[f"reactant-{i+1}"] = ''  # 填充空值

        # 填充其他组分（每个标签独立）
        for col, label in column_roles['other']:
            val = row[col]
            components = []
            if pd.notna(val):
                components = split_multi_smiles(_normalize_smiles(str(val)))
            for i, smi in enumerate(components):
                new_row[f"{label}-{i+1}"] = smi
            for i in range(len(components), max_counts['other'].get(label, 0)):
                new_row[f"{label}-{i+1}"] = ''

        # 填充数值列
        for col, label in column_roles['numeric']:
            new_row[label] = row[col] if pd.notna(row[col]) else ''

        # 填充产物
        product_col, product_label = column_roles['product']
        new_row[product_label] = _normalize_smiles(str(row[product_col])) if pd.notna(row[product_col]) else ''

        # 填充标签
        label_col, label_label = column_roles['label']
        new_row[label_label] = row[label_col]

        normalized_data.append(new_row)

    # 步骤 5: 生成 DataFrame 并保存
    normalized_df = pd.DataFrame(normalized_data, columns=normalized_columns)
    csv_path = Path(project_dir) / "规范数据集.csv"
    normalized_df.to_csv(csv_path, index=False, encoding="utf-8-sig")

    return normalized_df, csv_path
```

**风险**:
- ⚠️ **性能问题**: 大数据集（>10000 行）时需要优化循环（使用 pandas 向量化操作）
- ⚠️ **内存占用**: 拆分后列数可能激增（需要提前估算并警告用户）

---

### 步骤 4: 指定描述符

**可行性**: ⭐⭐⭐⭐☆ 中高
**技术难度**: ⭐⭐⭐⭐☆
**现有实现**: 已完成 50%

**分析**:
- ✅ **已有功能**:
  - 描述符选择（第 944-976 行）
  - DRFP 反应物-产物固定模式（`universal/feature_builder.py` 中已实现）

- ⚠️ **需要新增**:
  1. **拼接顺序可交互编辑**（横向拼接描述符）
  2. **标签选择性嵌入**（用户可排除某些标签）
  3. **配置预览与确认机制**

**技术难点**:

1. **用户界面设计**:
   - 如何让用户直观地编辑拼接顺序？
   - 推荐方案：打印默认配置 → 询问是否修改 → 逐个描述符交互

2. **配置存储**:
   - 需要维护描述符配置字典：
     ```python
     descriptor_configs = {
         'morgan': {
             'mode': 'horizontal_concat',  # 横向拼接
             'order': ['reactant', 'solvent', 'catalyst', 'product'],  # 拼接顺序
             'exclude_labels': []  # 排除的标签
         },
         'maf': {
             'mode': 'pointwise_sum',  # 逐点加和
             'exclude_labels': ['temperature']
         },
         'drfp': {
             'mode': 'fixed_reaction',  # 固定反应模式
             'editable': False
         }
     }
     ```

3. **与现有描述符代码集成**:
   - 需要修改 `universal/feature_builder.py` 中的 `build_universal_features()` 函数
   - 传入配置字典并据此调整拼接逻辑

**实现建议**:
```python
def configure_descriptors(descriptors, smiles_roles, numeric_cols):
    """交互式配置描述符嵌入策略。

    Args:
        descriptors: 用户选择的描述符列表
        smiles_roles: {'reactant': [...], 'product': [...], 'other': [...]}
        numeric_cols: 数值列列表（不参与描述符嵌入）

    Returns:
        descriptor_configs: {descriptor_name: config_dict}
    """
    configs = {}

    for desc in descriptors:
        print(f"\n[配置] 描述符: {desc}")

        if desc == 'drfp':
            print("  → DRFP 使用固定反应模式（reactant -> product），无法修改。")
            configs[desc] = {'mode': 'fixed_reaction', 'editable': False}
            continue

        if desc in ['morgan', 'maccs', 'rdkit2d', 'fisd', 'molmetalm']:
            # 横向拼接模式
            all_labels = smiles_roles['reactant'] + smiles_roles['other'] + smiles_roles['product']
            default_order = smiles_roles['reactant'] + smiles_roles['other'] + smiles_roles['product']

            print(f"  默认拼接顺序: {' -> '.join(default_order)}")
            modify = _ask_optional("  是否修改拼接顺序或排除某些标签？[y/N]", default="N").lower()

            if modify in ('y', 'yes'):
                # 交互式修改
                print("  请输入新的拼接顺序（空格分隔标签名，省略的标签将被排除）：")
                new_order_raw = _ask_optional(f"  拼接顺序（可选标签: {', '.join(all_labels)}）", default=' '.join(default_order))
                new_order = new_order_raw.split()
                exclude_labels = [l for l in all_labels if l not in new_order]

                configs[desc] = {
                    'mode': 'horizontal_concat',
                    'order': new_order,
                    'exclude_labels': exclude_labels
                }
                print(f"  → 拼接顺序确认: {' -> '.join(new_order)}")
                if exclude_labels:
                    print(f"  → 排除的标签: {', '.join(exclude_labels)}")
            else:
                configs[desc] = {
                    'mode': 'horizontal_concat',
                    'order': default_order,
                    'exclude_labels': []
                }

        elif desc == 'maf':
            # 逐点加和模式
            all_labels = smiles_roles['reactant'] + smiles_roles['other'] + smiles_roles['product']
            print(f"  默认嵌入所有 SMILES 标签: {', '.join(all_labels)}")
            modify = _ask_optional("  是否排除某些标签？[y/N]", default="N").lower()

            if modify in ('y', 'yes'):
                exclude_raw = _ask_optional(f"  排除的标签（空格分隔，可选: {', '.join(all_labels)}）")
                exclude_labels = exclude_raw.split()
                configs[desc] = {
                    'mode': 'pointwise_sum',
                    'exclude_labels': exclude_labels
                }
                print(f"  → 排除的标签: {', '.join(exclude_labels)}")
            else:
                configs[desc] = {'mode': 'pointwise_sum', 'exclude_labels': []}

    return configs
```

---

### 步骤 5: 后续步骤

**可行性**: ✅ 高
**技术难度**: ⭐☆☆☆☆
**现有实现**: 已完成 100%

**分析**:
- 选择模型（第 979-990 行）
- 数据集来源声明（第 993-1001 行）
- 命令行确认（第 1014-1059 行）
- 原步骤 11（未实现功能）可安全废弃

---

## 需求完整性检查

### 遗漏的边界情况

1. **❌ 缺失: 列角色冲突检测**
   - 问题：用户可能将同一列同时指定为反应物和产物
   - 建议：在收集完所有列角色后，检查是否有重复并报错

2. **❌ 缺失: 标签名称冲突检测**
   - 问题：用户可能为不同列指定相同的标签名称（如两个 `solvent`）
   - 建议：维护已使用标签名集合，输入时检查重复

3. **❌ 缺失: 空数据集处理**
   - 问题：如果所有行都被标记为非法，规范数据集将为空
   - 建议：生成规范数据集前检查有效行数，若为 0 则提示用户并退出

4. **❌ 缺失: 大数据集性能警告**
   - 问题：拆分 SMILES 后列数可能激增（如 1000 行 × 平均 5 个反应物 = 5000 列数据点）
   - 建议：在步骤 3.2 前预估列数，超过阈值（如 50 列）时警告用户

5. **⚠️ 不明确: 非法值判定优先级**
   - 问题：如果某行同时在标签列和 SMILES 列存在非法值，报告中如何展示？
   - 建议：明确"一行可以有多个非法原因"，报告中合并展示

### 逻辑漏洞

1. **⚠️ 漏洞: 步骤 2.2 中"非法行依然参与后续检验"存在歧义**
   - 问题：如果某行标签列非法，后续 SMILES 验证是否仍检查该行？
   - **建议的解释**: 应该继续检查（收集完整的错误信息），但在生成规范数据集时一并排除
   - 用户体验优化：一次性告诉用户所有错误，而非逐步修复

2. **⚠️ 漏洞: 步骤 3.2 规范数据集的列顺序定义不完整**
   - 问题：需求文档中提到"其他组分排列顺序与用户输入顺序一致"，但如果用户输入了 3 次 `solvent`（每次选择不同原始列），规范数据集中 `solvent-1`, `solvent-2`, `solvent-3` 的顺序应如何确定？
   - 建议：明确为"按原始列在数据集中的列序号排列"或"按用户输入时间顺序排列"

### 用户体验问题

1. **⚠️ 繁琐: 步骤 2.5 和 2.6 的多次输入确认**
   - 问题：如果用户有 5 个其他组分列，需要交互 5 次
   - 建议：提供"批量输入模式"选项（一次性输入多列，自动编号）

2. **❌ 缺失: 数据预览功能**
   - 问题：用户在确认配置前无法预览规范数据集的样子
   - 建议：在步骤 3.2 生成规范数据集后，打印前 5 行供用户确认

3. **❌ 缺失: 错误修复引导**
   - 问题：非法输入报告生成后，用户如何修复原始数据？
   - 建议：报告中增加"建议修复方案"章节（如"请在 Excel 中打开原始 CSV，定位到第 X 行..."）

---

## 技术难点与风险

### 高风险项（需重点关注）

#### 1. 规范数据集生成的性能优化 ⚠️

**风险等级**: 高
**影响**: 大数据集（>5000 行）可能导致运行时间 > 10 分钟

**解决方案**:
- 使用 pandas 向量化操作替代逐行循环
- 对 `split_multi_smiles()` 和 `_normalize_smiles()` 使用 `apply()` 批量处理
- 考虑使用 `numba` 或 `cython` 加速关键循环

**示例优化**:
```python
# 慢速版本（逐行循环）
for idx, row in df.iterrows():
    normalized = _normalize_smiles(str(row[col]))
    # ...

# 优化版本（向量化）
df[col + '_normalized'] = df[col].apply(lambda x: _normalize_smiles(str(x)) if pd.notna(x) else '')
```

#### 2. 描述符配置的状态管理 ⚠️

**风险等级**: 中
**影响**: 配置错误可能导致特征矩阵维度不匹配

**解决方案**:
- 在 `build_universal_features()` 函数中增加配置验证
- 输出配置摘要供用户确认
- 将配置保存到项目文件夹（`descriptor_config.json`）供调试

#### 3. 非法行跟踪的内存占用 ⚠️

**风险等级**: 低
**影响**: 百万行数据集可能占用 >100MB 内存

**解决方案**:
- 使用 `defaultdict(list)` 存储非法行
- 仅存储行号和错误类型码（而非完整错误消息），生成报告时再拼接

---

### 中风险项

#### 1. 用户输入错误容错 ⚠️

**风险**: 用户可能输入不存在的列名/序号，导致程序崩溃

**解决方案**: 已有 `_resolve_one()` 函数处理，但需要增强：
- 支持模糊匹配（如用户输入 `Reactnt` 时提示"是否指 `Reactant`？"）
- 提供撤销功能（输入 `undo` 返回上一步）

#### 2. Markdown 表格渲染兼容性 ⚠️

**风险**: 单元格内容包含特殊字符（如 `|`、`\n`）可能破坏表格格式

**解决方案**:
- 对单元格内容进行转义（`|` → `\|`，`\n` → `<br>`）
- 使用成熟库（如 `tabulate`）生成 Markdown 表格

---

## 架构兼容性分析

### 与现有代码的兼容性: ✅ 良好

**现有架构优势**:
1. **模块化设计**: 验证逻辑、描述符构建、模型训练分离清晰
2. **向导模式**: 已有交互式输入框架，易于扩展
3. **列角色支持**: 已实现 `reactant_cols`、`product_cols`、`other_cols` 参数传递

**需要修改的模块**:

| 模块 | 文件路径 | 修改类型 | 工作量估算 |
|------|---------|----------|-----------|
| 向导流程 | `yonod.py` (wizard 函数) | 重构 | 3-5 天 |
| 数据加载 | `yonod/universal/csv_loader.py` | 扩展 | 1-2 天 |
| 特征构建 | `yonod/universal/feature_builder.py` | 扩展 | 2-3 天 |
| 报告生成 | 新建 `yonod/universal/report_generator.py` | 新增 | 2-3 天 |

**总工作量估算**: 8-13 天（单人全职开发）

---

### 对现有功能的影响

**✅ 无影响的功能**:
- CLI 模式（`python yonod.py --csv ...`）
- 模型训练与评估
- 结果报告生成

**⚠️ 需要适配的功能**:
1. **特征构建逻辑**:
   - 需要支持描述符配置字典
   - `build_universal_features()` 函数需要增加 `descriptor_configs` 参数

2. **数据加载逻辑**:
   - `load_csv_with_roles()` 需要返回标签名称映射
   - 需要支持自定义列标签名

**建议的迁移策略**:
1. 保持现有 CLI 模式不变（向后兼容）
2. 新增 `--use-wizard-v2` 参数启用重构后的向导流程
3. 两个版本并行运行 1-2 个月，确认无问题后废弃旧版本

---

## 改进建议

### 对需求文档的改进建议

#### 1. 明确非法行处理策略 ⭐⭐⭐

**当前描述**（第 31-32 行）:
> 对于含有非法值的行，提示用户第 X 行因何而非法，无法作为输入，**并且在创建规范数据集时跳过这些行（而不是出现非法值就退出程序），但这些行依然参与到后续的数值合法性检验中**

**问题**:
- "依然参与后续检验"与"跳过这些行"矛盾
- 用户可能误解为"非法行的其他列依然有效"

**建议修改为**:
> 对于含有非法值的行，**继续检查该行的其他列以收集完整的错误信息**（避免用户反复修复），但在生成规范数据集时**整行排除**。所有非法值将在步骤 3.1 的排除报告中一次性展示。

---

#### 2. 增加列命名规范约束 ⭐⭐⭐

**当前描述**（第 26、39、51、77 行）:
> 标签名称必须仅使用英文与符号，否则要求用户重新输入

**问题**:
- "符号"范围不明确（是否包括空格、中文标点？）
- 未说明是否允许数字

**建议修改为**:
> 标签名称规则：
> - 仅允许英文字母（a-z, A-Z）、数字（0-9）、下划线（_）、连字符（-）
> - 不允许空格、中文字符、特殊符号（如 `@`, `#`, `$` 等）
> - 示例合法标签：`solvent`, `Temp_K`, `catalyst-1`
> - 示例非法标签：`溶剂`, `temp (K)`, `cat@1`

---

#### 3. 补充规范数据集列顺序的详细规则 ⭐⭐

**当前描述**（第 117-119 行）:
> 然后是其他组分（others），创建条件与反应物一致，在表格中排列的顺序与用户输入的顺序一致

**问题**:
- "用户输入的顺序"指什么？（输入时间顺序 vs. 原始列序号）
- 如果同一标签对应多列（如用户选择了 `solvent-A` 和 `solvent-B` 两列），规范数据集中 `solvent-1`, `solvent-2` 的来源如何确定？

**建议修改为**:
> 其他组分列排列规则：
> 1. **标签排列顺序**: 按用户输入标签的时间顺序（先输入 `solvent` 再输入 `catalyst`，则 `solvent-X` 列在前）
> 2. **同标签内部顺序**:
>    - 如果该标签对应单列，按拆分后的组分顺序编号（`solvent-1`, `solvent-2`, ...）
>    - 如果该标签对应多列（用户多次选择同名标签），按原始列在数据集中的列序号排列

---

#### 4. 增加数据预览与确认环节 ⭐⭐

**当前缺失**: 步骤 3.2 生成规范数据集后，直接进入步骤 4

**建议新增**:
> #### 3.3 预览规范数据集
>
> 在生成规范数据集后，打印以下信息供用户确认：
> 1. 规范数据集行数（有效行数 vs. 原始行数）
> 2. 规范数据集列数及列名
> 3. 前 5 行数据预览（Markdown 表格格式）
> 4. 文件保存路径
>
> 询问用户："是否确认规范数据集无误？[Y/n]"
> - 若用户输入 `n`，提示"请修改原始数据后重新运行"并退出
> - 若用户输入 `Y` 或直接回车，继续步骤 4

---

### 对实现方案的优化建议

#### 1. 引入配置文件机制 ⭐⭐⭐

**问题**: 当前所有配置通过交互式输入收集，无法复用

**建议**:
- 在项目文件夹下生成 `yonod_config.json`，保存完整配置（列角色、标签名称、描述符配置等）
- 用户可编辑该文件后通过 `python yonod.py --config yonod_config.json` 直接执行
- 支持"导出配置模板"功能（`python yonod.py --export-config-template`）

**优势**:
- 方便批量处理多个数据集
- 便于分享配置给团队成员
- 支持 CI/CD 自动化测试

---

#### 2. 增加撤销/重做功能 ⭐⭐

**问题**: 用户在步骤 7 发现步骤 3 的配置有误，只能退出重新运行

**建议**:
- 维护配置历史栈 `config_history: list[dict]`
- 支持命令：
  - `undo`: 返回上一步
  - `redo`: 前进到下一步
  - `goto <step>`: 跳转到指定步骤
  - `restart`: 重新开始

**实现提示**:
```python
class WizardState:
    def __init__(self):
        self.current_step = 1
        self.config = {}
        self.history = []

    def save_checkpoint(self):
        self.history.append((self.current_step, copy.deepcopy(self.config)))

    def undo(self):
        if self.history:
            self.current_step, self.config = self.history.pop()
```

---

#### 3. 批量输入模式优化 ⭐

**问题**: 步骤 2.5 和 2.6 需要多次交互输入，体验繁琐

**建议**: 提供两种模式供用户选择

**模式 1: 逐列交互模式**（适合初学者）
```
[输入其他组分列 1/N]
  列名: solvent
  标签名 [默认: solvent]:
  是否继续输入？[y/N]: y

[输入其他组分列 2/N]
  列名: catalyst
  标签名 [默认: catalyst]:
  是否继续输入？[y/N]: n
```

**模式 2: 批量输入模式**（适合熟练用户）
```
  请输入所有其他组分列（格式: 列名:标签名，多列用空格分隔）
  示例: solvent:solvent catalyst:cat base:base
  输入: solvent:solvent catalyst:catalyst
  → 解析结果:
    - solvent → 标签: solvent
    - catalyst → 标签: catalyst
  确认？[Y/n]:
```

---

#### 4. 非法输入报告增强 ⭐⭐

**当前需求**: 仅展示非法行及原因

**建议增强**:
1. **错误分类统计**（饼图或柱状图，如果生成 HTML 报告）
   ```
   错误类型分布:
   - 标签列非数值: 5 行 (50%)
   - SMILES 格式错误: 3 行 (30%)
   - 产物列多 SMILES: 2 行 (20%)
   ```

2. **修复建议**
   ```
   ## 修复建议

   1. 标签列非数值（5 行）
      - 问题: 标签列包含文本或空值
      - 解决: 在 Excel 中将以下行的标签列改为数值
      - 行号: 12, 34, 56, 78, 90

   2. SMILES 格式错误（3 行）
      - 问题: SMILES 字符串无法被 RDKit 解析
      - 解决: 检查是否有截断、乱码或占位符（如 "N/A", "—"）
      - 行号: 15, 23, 67
   ```

3. **导出非法行子集** CSV（方便用户在 Excel 中批量修复）
   ```
   非法行数据已导出至: 项目文件夹/非法行数据.csv
   修复后请重新运行 YONOD。
   ```

---

## 实施建议

### 分阶段实施策略

#### 第一阶段（核心功能，优先级: P0）
**时间估算**: 5-7 天

1. **重构向导流程（步骤 1-2）**
   - 实现标签名称自定义
   - 实现非法行收集机制（不立即退出）
   - 实现产物列单 SMILES 校验

2. **实现规范数据集生成（步骤 3.2）**
   - SMILES 拆分与填充逻辑
   - 动态列数计算
   - CSV 保存

3. **实现非法输入报告生成（步骤 3.1）**
   - Markdown 表格渲染
   - 非法值加粗标记

**验收标准**:
- 能够处理包含非法值的数据集并生成报告
- 规范数据集格式符合需求文档

---

#### 第二阶段（描述符配置，优先级: P1）
**时间估算**: 3-4 天

1. **实现描述符配置交互（步骤 4）**
   - 拼接顺序编辑
   - 标签排除功能

2. **集成描述符配置到特征构建**
   - 修改 `build_universal_features()`
   - 支持配置字典传参

**验收标准**:
- 用户可自定义描述符拼接策略
- 特征矩阵维度与配置一致

---

#### 第三阶段（体验优化，优先级: P2）
**时间估算**: 2-3 天

1. **配置文件导出**
2. **撤销/重做功能**
3. **批量输入模式**
4. **数据预览功能**

**验收标准**:
- 用户可导出并复用配置
- 支持返回上一步修改配置

---

### 测试策略

#### 1. 单元测试（覆盖率目标: >80%）

**关键测试点**:
- `_normalize_smiles()`: 各类分隔符处理、JSON 数组、空值
- `split_multi_smiles()`: 优先级拆分、边界情况
- `_validate_label_with_collection()`: 数值验证、JSON 格式、非法行收集
- `_validate_smiles_with_collection()`: RDKit 验证、多组分、非法行收集
- `generate_normalized_dataset()`: 列数计算、数据填充、空值处理

#### 2. 集成测试

**测试数据集**:
1. **小数据集**（10 行 × 5 列）: 验证基本流程
2. **含非法值数据集**（50 行，20% 非法）: 验证错误收集与报告生成
3. **大数据集**（10000 行 × 20 列）: 性能压测
4. **边界数据集**（所有行非法、单行数据集、空数据集）: 鲁棒性测试

#### 3. 用户验收测试（UAT）

**邀请对象**: 课题组成员、潜在用户

**测试任务**:
1. 使用真实数据集完成完整流程
2. 记录操作耗时和困惑点
3. 填写用户体验问卷

---

### 文档更新

**需要同步更新的文档**:
1. `README.md`: 增加"规范数据集生成"章节
2. `teaching.md`: 更新向导流程说明（如果有）
3. 新建 `docs/数据集规范.md`: 详细说明规范数据集格式要求
4. 新建 `docs/描述符配置指南.md`: 说明如何编辑描述符拼接策略

---

## 附录：需要用户进一步明确的问题

### 问题清单

1. **Q1: 标签名称冲突时如何处理？**
   - 场景：用户为两列都输入标签名 `solvent`
   - 选项 A：自动编号（`solvent`, `solvent_2`）
   - 选项 B：报错要求重新输入
   - **建议**: 选项 B（保证语义清晰）

2. **Q2: 非法行过多时（>50%）是否给予警告？**
   - 场景：1000 行数据集有 600 行非法
   - 建议：打印警告并询问"是否继续？数据质量可能影响建模效果"

3. **Q3: 规范数据集列数过多时（>100 列）是否限制？**
   - 场景：某数据集拆分后产生 200 列
   - 建议：打印警告，建议用户合并某些组分或使用 MAF 描述符（逐点加和）

4. **Q4: 步骤 4 中"others 下面的多个标签"是否包括数值列？**
   - 需求文档第 141 行提到"others 下面可能有多个标签"
   - 但数值列不参与描述符嵌入，是否需要在配置中排除？
   - **建议**: 明确为"仅指 SMILES 类型的 other 列，数值列单独处理"

5. **Q5: DRFP 描述符是否支持其他参与者（催化剂等）？**
   - 当前需求文档第 149 行规定"固定为 reactant->product"
   - 但 DRFP 论文中支持 `reactant.catalyst>>product` 格式
   - 建议：允许用户选择"是否包含催化剂"（默认不包含）

---

## 总结

该重构需求**整体可行且必要**，能够显著提升 YONOD 的数据处理能力和用户体验。关键成功因素：

1. ✅ **架构兼容**: 与现有代码高度契合，无需大规模重写
2. ⚠️ **实施复杂度**: 规范数据集生成逻辑需要精心设计（尤其是性能优化）
3. ⚠️ **用户体验**: 交互流程较长，需要提供撤销、配置导出等辅助功能
4. ✅ **技术可行性**: 核心依赖（RDKit、pandas）已存在，无新增外部依赖

**推荐实施**: 按分阶段策略推进，先完成核心功能（P0），验证无误后再优化体验（P1-P2）。

---

**报告编制**: Claude Sonnet 4.5
**审核建议**: 请课题组负责人审阅"需要用户进一步明确的问题"章节，明确后再进入开发阶段。
