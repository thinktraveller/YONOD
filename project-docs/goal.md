# 项目目标文档

## 项目愿景
将 YONOD 从“描述符 × 模型的性能对比工具”推进为一个受 `VJETHBKM` 文献启发、具备可复现实验流程、自动化报告能力和论文/答辩级结果表达的有机反应产率预测 benchmark 平台。

## 背景与动机
YONOD 当前已经支持多种有机反应数据输入、多类分子/反应描述符以及多种机器学习模型组合。基于对 Zotero 文献 `VJETHBKM`（《Yield Smarter, Not Harder: Good Practices for Machine Learning of Reaction Outcomes》）的精读，项目下一步需要优先补强评估范式，而不仅是继续堆叠更复杂的描述符或模型。

Z-AIHub 中该文献已有主文和 SI 两个向量化附件：主文 11 页，SI 13 页。文献元数据记录 DOI 为 `10.1021/jacs.6c02213`，并指向官方数据与代码资源 `10.3929/ethz-c-000800856`，可作为后续复现的主要依据。

该文献对本项目的关键指导意义包括：
- 随机 5 折交叉验证容易高估模型对新底物、新条件或新反应空间的泛化能力。
- 复杂描述符不一定天然优于低成本 2D 指纹或简单统计基线，必须通过公平对照证明其增益。
- 研究结论应同时报告性能、稳定性、计算成本和适用边界。
- 推荐采用不同难度层级的评估设计，例如随机划分、单维留出、组合留出和外部验证。
- 复现工作的价值不只是得到相近数值，更是把论文方法转化为 YONOD 可复用、可解释、可展示的 benchmark workflow。

## 核心目标
1. 以“先 A 后逐步逼近 B/C”的方式复现 `VJETHBKM`：第一阶段复现核心工作流和趋势结论，第二阶段尽量逼近主文主要表图数值，第三阶段视资源情况扩展到主文与 SI 的完整实验矩阵。
2. 建立 YONOD 的可复用 benchmark pipeline，使后续不同数据集、描述符、模型和划分策略能够用统一入口运行、记录、比较和复查。
3. 优先把创新点落在“自动化报告/可复现实验工具化”：自动汇总实验配置、数据划分、指标、图表、运行成本、结论模板和与论文结果的差异。
4. 在复现过程中保留 `VJETHBKM` 的研究问题主线：比较简单 baseline 与复杂描述符，评估模型泛化能力，分析描述符复杂度和计算成本之间的收益权衡。
5. 形成能够直接服务于大创答辩、潜在论文写作和软件工具展示的结果包，包括 benchmark 结果表、关键可视化、复现说明和项目创新点总结。
6. 将来自 Polaris Method Comparison 与 Expansion ML Comparison 的严谨模型比较架构纳入 YONOD：统一切分、分组重复交叉验证、逐折预测留存、统计比较、可恢复实验编排，以及 LightGBM 基线。
7. 将描述符生成与模型训练彻底解耦：每次运行先计算本次配置选中的全部描述符并持久化，再由建模阶段只读取这些已落盘的描述符文件，避免因模型组合或重复建模而重复计算描述符。
8. 将 MFP 与 OHE 纳入统一、可发现的特征库：普通 `main.py`、JSON 配置和交互式 `yonod.py` 均可选择它们；其中 OHE 始终遵守按训练折拟合的无泄漏边界。

## 目标用户
主要用户是 YONOD 项目作者和团队成员，他们需要一个清晰、可复现、可解释的研究闭环来支撑大创结题、答辩展示和后续论文探索。

次要用户包括课题组成员、评审老师以及希望复用 YONOD 做反应产率预测 benchmark 的同学。他们关心的是流程是否可信、结果是否可复查、图表是否能说明问题，以及工具是否能迁移到其他反应数据集。

## 期望成果
项目最终应形成一个以 `VJETHBKM` 为参照的 YONOD benchmark 复现与报告体系。它既能复现论文的核心实验逻辑，也能沉淀为本项目自己的自动化 benchmark 能力。

期望成果包括：
- 一套可运行的 benchmark pipeline 目标定义，覆盖数据导入、描述符生成、模型训练、验证划分、指标统计和报告生成。
- 一组与 `VJETHBKM` 可对照的核心实验结果，优先呈现趋势一致性，再逐步追求主要表图数值接近。
- 一份自动化报告目标，要求输出论文/答辩可用的图表、指标表、运行配置、复现差异和结论摘要。
- 一个清晰的创新点表述：YONOD 的贡献重点不是单一新模型，而是把反应产率预测 benchmark 做成可复现、可比较、可展示的工具化流程。
- 一套面向模型公平比较的实验评估层：同一数据集和切分计划下，完整保存每个“描述符 × 模型 × repeat × fold”的预测、指标、配置与状态，并将统计结论独立于训练过程生成。
- 一套可复用的描述符产物层：每次结果目录下均保留 `descriptors/` 子目录，其中按描述符分别保存特征矩阵、样本对应关系、有效性信息、特征结构和配置元数据；相同数据与配置可直接复用，且建模无需再次调用描述符计算。
- 一套适用于普通反应预测任务的 MFP/OHE 使用体验：用户可在配置或向导中显式选择组分/类别列和参数；系统能够区分静态描述符与折内转换器，并在报告中如实记录二者的特征语义与拟合范围。

## 成功标准
1. 复现层级达成：至少完成 `VJETHBKM` 核心工作流与趋势结论复现；若数据、代码和算力允许，再逼近主文主要表图，最后扩展 SI 完整矩阵。
2. Pipeline 可复用：同一套流程能够配置不同描述符、模型、数据集、划分策略和评价指标，并输出结构化结果。
3. 报告可直接使用：自动生成的结果应包含指标汇总表、预测可视化、描述符/模型对照、复现差异说明和可用于答辩/论文的结论文字。
4. 创新点明确：最终能够说明 YONOD 相比单纯复现实验的增量价值，即“自动化 benchmark 与报告工具化”如何降低复现实验成本、提高结果可追溯性，并支持后续数据集迁移。
5. 结论可审计：所有关键结果都应能追溯到数据版本、描述符配置、模型配置、划分策略、随机种子和运行输出。
6. 统一切分：一次实验生成并保存可复用的数据切分计划（split manifest）；参与同一比较的所有描述符与模型必须复用该计划。
7. 泛化评估：至少支持按反应相似性分组的重复交叉验证，默认验收配置为 5 次重复 × 5 折，并提供更小的 smoke 配置；训练集与验证集之间不得混入同一分组的相似反应。
8. 预测与统计可复查：每个折必须保存样本标识、真实值、预测值、描述符、模型、repeat、fold 和运行配置；独立模块可据此计算 R²、RMSE、MAE、置信区间、配对检验和 Tukey HSD 多重比较结果，并同时支持模型维度与描述符维度的比较。
9. 长实验可恢复：实验应以“描述符 / 模型 / repeat / fold”为最小可追踪单元，完成单元可被识别并跳过，失败单元须保留原因且可单独重跑。
10. 基线完整：在现有 RF、XGBoost、SVM、AutoGluon 等模型基础上加入 LightGBM，并确保它参与与其他模型相同的切分、指标和统计比较。
11. 阶段顺序明确：单次运行必须先完成本次配置所选描述符的计算或有效复用，再开始任何模型训练；任一模型训练入口不得自行重新计算描述符。
12. 产物结构稳定：每次结果目录的 `descriptors/` 下，每个所选描述符对应一个独立持久化文件；文件至少保存 `sample_id`、有效性标记、描述符特征矩阵、特征列或等价的特征结构说明、特征维度，以及足以重建该描述符的配置元数据。
13. 复用判定可审计：当输入数据身份与描述符配置一致时自动读取已有文件；任一者发生变化时自动重新计算相应描述符，不得静默复用过期产物。
14. 失败相互隔离：某个描述符计算失败时，只跳过该描述符及其全部模型任务；其他成功描述符仍可继续建模，最终报告必须列出失败描述符、失败阶段和原因。
15. 避免重复计算可验证：在同一次运行的多个模型、重复或折之间，同一描述符至多计算一次；再次执行同一数据与同一描述符配置时应显示命中并复用持久化产物。
16. 通用特征可安全使用：普通 JSON/CLI/向导任务能独立运行 MFP 或 OHE；OHE 的类别表、输出维度和保存的状态均只源于对应训练折，验证集未见值不会触发重新拟合或信息泄漏。

## 范围界定
### 包含
- 查询、整理并使用 `VJETHBKM` 主文、SI、官方数据与代码资源作为复现依据。
- 复现核心 workflow：反应组件特征化、描述符拼接、模型训练、交叉验证、指标比较和结果可视化。
- 优先实现或规划 YONOD benchmark pipeline 的自动化报告能力。
- 对照 `VJETHBKM` 的关键思想：低成本 baseline、描述符复杂度比较、泛化验证、成本-收益分析。
- 产出答辩/论文可用的图表、表格和结论摘要。
- 建立可版本化、可复用的 split manifest，并让同一比较中的全部候选方案共享这一切分定义。
- 针对反应相似性建立分组重复交叉验证；反应指纹相似性聚类与底物骨架/组分留出均可配置，分组依据、聚类/分桶参数和随机种子必须随实验配置保存。
- 保存逐折样本级预测与折级指标，支持从已有结果重建指标、置信区间、配对检验和 Tukey HSD 报告，无需重新训练。
- 建立按描述符、模型、重复次数和折拆分的实验编排与状态记录，支持中断后续跑、失败定位和局部重跑。
- 将 LightGBM 作为新增的树模型回归基线，纳入现有描述符与模型比较矩阵。
- 在每次结果目录下建立 `descriptors/` 产物目录，并将“描述符预计算/复用”设置为独立于建模的前置阶段。
- 仅预计算本次配置选中的全部描述符；每个描述符独立落盘，并保存样本对齐、有效性、特征结构和配置元数据。
- 建模阶段只通过已落盘描述符文件获得特征；新增模型、增加重复次数或重跑某个折时不得触发描述符重新计算。
- 对已有描述符产物执行数据与描述符配置一致性校验：一致时自动复用，不一致时仅重算受影响的描述符。
- 将描述符计算失败纳入运行状态与最终报告；失败只影响对应描述符，不阻塞其余描述符及模型任务。
- 将 MFP 作为可参数化的静态分子计数指纹、将 OHE 作为可序列化的折内类别特征转换器纳入通用特征目录；同时维持两者不同的特征生命周期。

### 排除
- 不把目标设定为立即完整复现所有主文与 SI 细节；完整矩阵属于后续扩展阶段。
- 不默认追求大型深度学习模型训练或全新模型发明。
- 不默认开发全新的前端系统、商业化部署或与复现目标无关的数据工程重构。
- 不以单次随机 5 折交叉验证的高分作为主要创新点。
- 不直接照搬 ADME 任务的数据结构、标签变换或大型预训练图模型；只有经过反应级特征适配和资源评估后，才将其列入后续模型扩展。
- 不在一次运行中强制计算未被本次配置选中的描述符。
- 不把进程内临时缓存视为完成持久化；只有结果目录 `descriptors/` 中通过一致性校验的文件才可作为建模输入。
- 本目标不统一改变各运行入口现有的无效样本处置语义；描述符文件必须如实保留有效性标记，后续建模按对应入口既有规则处理并可审计。

## 约束条件
- 复现工作应先保证最小闭环完成，再追求数值精度和实验矩阵完整性。
- 当前首要约束是找到有说服力的项目创新点，而不是单纯堆叠模型或描述符。
- 数据、代码、补充材料、运行环境和算力可用性会影响复现层级，因此目标采用分阶段推进。
- YONOD 现有能力应被优先复用，包括 Morgan、MACCS/ATMOMACCS、RDKit2D、DRFP、FISD、MolMetaLM、RF、XGBoost、SVM、AutoGluon 等已有模块。
- 反应相似性分组必须在不泄漏验证折信息的前提下完成；反应指纹相似性聚类与底物骨架/组分留出作为可配置策略，具体阈值、聚类规则与 fallback 行为须随实验配置保存。
- 大规模重复交叉验证会增加运行时间和磁盘占用，逐折预测与中间状态需采用结构化、可审计且可恢复的存储方式。
- 最终成果需要同时兼顾研究可信度、答辩表达和工具可复用性。
- 描述符产物可能体积较大，持久化方案必须保证样本顺序与 `sample_id` 可核对，并避免不完整文件被误判为可复用产物。
- 复用身份至少由输入数据身份和描述符配置共同决定；模型类型、模型超参数、交叉验证重复次数与折数不应导致描述符失效或重算。
- 若建模需要拼接数值辅助列，可以在读取描述符文件后完成，但不得因此再次调用描述符计算。

## 阶段规划
### 阶段一：最小闭环复现
目标是跑通 `VJETHBKM` 的核心思路，而不是一开始追求所有数值完全一致。该阶段应确认数据来源、复现实验入口、关键描述符、核心模型、基础验证方式和基本指标输出。

### 阶段二：主文结果逼近
在最小闭环稳定后，优先复现主文中最能支撑结论的表图和趋势，例如简单描述符与复杂描述符的性能比较、RF 基准模型表现、5×5 交叉验证或主要泛化验证。

### 阶段三：YONOD 工具化创新
把复现过程抽象为 YONOD 的 benchmark pipeline，并强化自动化报告能力，使实验结果可追溯、可重复、可展示、可迁移。本阶段新增统一 split manifest、反应相似性分组重复交叉验证、逐折预测库、统计比较模块、可恢复实验编排和 LightGBM 基线。

## 严谨模型比较架构目标
本轮已明确采纳 Polaris Method Comparison 的比较规范，以及 Expansion ML Comparison 的实验编排与结果留存思路。YONOD 仍以反应产率预测为任务对象，不迁移 ADME 数据或将其特定模型组合直接当作本项目目标。

### 数据切分与验证
- 每次 benchmark 先生成唯一、可复用的 split manifest；它记录数据版本、样本标识、分组标识、训练/验证归属、repeat、fold、随机种子与生成参数。
- 默认评估采用按反应相似性分组的 5 次重复 × 5 折交叉验证，并提供更小的 smoke 配置；同一 reaction group 不得同时出现在同一折的训练集与验证集。
- 反应指纹相似性聚类与底物骨架/组分留出均需支持配置；相似性阈值、聚类规则与 fallback 行为必须写入 split manifest。

### 实验编排与结果留存
- 一个实验单元由 `descriptor`、`model`、`repeat`、`fold` 唯一标识；它应记录状态（待运行、运行中、成功、失败、跳过）、输入配置、运行时间和错误摘要。
- 中断重启时，只重跑未完成或用户显式指定的单元；已经成功且配置一致的单元不得被静默覆盖。
- 成功单元必须落盘样本级预测与折级指标。样本级记录至少包括 `sample_id`、`y_true`、`y_pred`、`descriptor`、`model`、`repeat`、`fold`、`split_id` 与配置标识。

### 描述符预计算与持久化
- 整体流程拆成两个具有明确边界的阶段：阶段 A 处理本次配置选中的全部描述符，阶段 B 只消费阶段 A 已成功落盘或校验通过的文件进行建模。
- 结果目录包含 `descriptors/`，并按描述符一对一保存独立文件。每个文件至少包含描述符特征矩阵、`sample_id`、有效性标记、特征列或等价结构说明、特征维度及完整描述符配置元数据。
- 描述符产物的有效身份由输入数据与描述符配置共同决定；身份一致则自动复用，身份变化则自动重算对应描述符。模型或交叉验证配置的变化不得使描述符缓存失效。
- 即使描述符刚在阶段 A 中生成，阶段 B 也应通过持久化文件读取特征，以确保“离线预计算后单独建模”与“一次性完整运行”遵守同一输入契约。
- 描述符失败采用隔离策略：记录描述符名称和失败原因，跳过它对应的所有模型任务，其余描述符继续执行；最终报告集中说明成功、复用、重算、失败和跳过状态。
- 单个描述符文件内部必须能够校验样本对应关系；无效样本不可通过静默删行破坏 `sample_id` 对齐，具体训练取样仍遵循相应运行入口的既有规则。

### 指标与统计比较
- 训练阶段只负责产出预测及运行元数据；指标与统计模块从统一预测库独立计算结果。
- 指标至少覆盖 R²、RMSE、MAE 及其跨折分布；报告给出置信区间。
- 对共享切分的方案使用配对检验，并采用 Tukey HSD 处理多模型/多方案比较；统计报告同时覆盖“同一描述符下的模型比较”和“同一模型下的描述符比较”，结论应区分“显著更优”“无显著差异”“显著较差”。

### 模型基线
- 保留既有模型矩阵，新增 LightGBM 回归基线。
- LightGBM 与其他候选模型使用同一特征输入、同一 split manifest、同一指标定义和同一统计比较流程，避免不公平比较。

### 阶段四：答辩/论文成果整理
将 benchmark 结果转化为图表、结论段落、复现差异说明和项目创新点总结，形成大创答辩与潜在论文写作可直接使用的材料。

## 文件夹整理目标补充
本次文件夹整理是 `VJETHBKM` 复现与 YONOD benchmark 工具化目标的配套工作，核心原则是“三者兼顾但先不大改结构”：既让日常开发更清爽，又让大创/答辩材料更规范，同时为后续论文复现实验留下可追溯的目录边界。

整理目标包括：
- 按“代码 / 数据 / 文献 / 实验结果 / 项目文档”建立清晰分区认知，并尽量沿用当前已有目录，不进行一次性大搬迁。
- 保持 `yonod/` 作为核心源码包，`dataset/` 作为数据目录，`WEIGHTS/` 作为本地权重目录，`docs/参考文献/` 作为文献与补充材料目录，`project-docs/` 作为目标、计划、日志和教学说明目录。
- 对 `.vs`、`.vscode`、`.pytest_cache`、`__pycache__` 等 IDE 或运行缓存，优先通过 `.gitignore` 管理；若要删除，必须先确认它们不影响当前开发环境恢复。
- 对 `_verify/` 等临时验证脚本不能默认删除，应逐项确认用途、Git 跟踪状态、是否仍能解释历史修复，再决定保留、归档或删除。
- 保持现有运行入口和代码引用有效，尤其不能在未同步修改 README、脚本入口、导入路径和示例配置的情况下移动 `main.py`、`yonod.py`、`dataset/` 或 `WEIGHTS/`。

整理范围与安全边界：
- 包含：目录职责说明、命名建议、忽略规则审计、缓存/IDE 文件识别、临时验证材料评估、文献/数据/结果/项目文档的分区建议。
- 排除：未经确认的批量删除、源码入口重构、数据集重命名、权重文件搬迁、破坏历史可复查性的清理。
- 删除前必须逐项确认文件用途、是否被 Git 跟踪、是否被 README/脚本/配置引用、是否可由运行流程重新生成。

成功标准：
1. 项目根目录能够一眼区分核心代码、数据、文献、实验产物、项目管理文档和本地缓存。
2. 清理后不影响现有命令入口、模块导入、示例配置和已规划的 benchmark pipeline。
3. 不把 IDE 缓存、Python 缓存、大模型权重、临时结果等不适合版本管理的内容误纳入 Git。
4. 对可能删除的内容有可追溯判断依据，而不是简单按名称批量删除。
5. 后续 project-planner-cn 可以据此拆解为低风险的整理步骤、检查清单和执行顺序。

预期交付：
- 一份当前目录职责与建议结构说明。
- 一份可删除、应保留、需进一步确认的文件/目录清单。
- 一份整理执行前检查清单，覆盖 Git 状态、引用关系、可再生成性和回滚方式。
- 在用户确认后，再由实现/规划 agent 执行实际移动、归档或删除操作。

## 澄清问答记录
<!-- GOAL-QA-LOG-START -->

### QA-R001：确定下一阶段主目标
<!-- GOAL-QA-R001-START -->
- **提问时间**：2026-08-24 16:02
- **提问目的**：用户希望基于文献精读与当前项目内容明确下一步操作。本轮用于判断下一阶段应优先服务于研究可信度、工程功能完善，还是结题/论文表达。
- **问题列表**
  1. 你希望 YONOD 的下一阶段主要成果更偏向哪一种：A. 大创结题/答辩材料，B. 可发表的实验研究，C. 可复用的软件工具，还是 D. 三者兼顾但先做最小闭环？
  2. 基于 `VJETHBKM` 的启发，你更想优先验证哪类结论：A. 复杂描述符是否真的优于简单 baseline，B. 模型在未见底物/条件上的泛化能力，C. 不同描述符的成本-收益权衡，还是 D. 以上都要但分阶段完成？
  3. 你当前最现实的约束是什么：时间紧、算力有限、外部数据难找、需要尽快出图表/报告，还是代码稳定性更重要？
- **用户回答**
  1. D
  2. D
  3. 寻找可能的创新点
- **提炼结论**
  - 用户选择“三者兼顾但先做最小闭环”，说明下一阶段目标应兼顾大创结题/答辩、潜在论文研究与可复用软件工具，但执行顺序应先收敛到一个可完成、可验证的复现闭环。
  - 用户希望 `VJETHBKM` 启发下的三类结论都要，但需要分阶段推进：先做简单 baseline 与复杂描述符的公平对照，再推进泛化验证和成本-收益分析。
  - 用户当前最关心的是“寻找可能的创新点”，因此复现本身不是终点，而是用于发现 YONOD 可差异化贡献的研究入口。
- **影响的目标文档章节**
  - 核心目标
  - 期望成果
  - 成功标准
  - 范围界定
  - 约束条件
<!-- GOAL-QA-R001-END -->

### QA-R002：确定复现层级与创新落点
<!-- GOAL-QA-R002-START -->
- **提问时间**：2026-08-25 11:23
- **提问目的**：用户已确认希望三者兼顾并寻找创新点。本轮用于明确复现要达到的精度、创新点优先落在哪一层，以及最终成果以什么形式证明价值。
- **问题列表**
  1. 你希望复现 `VJETHBKM` 到哪一层：A. 只复现核心工作流和趋势结论，B. 尽量复现论文主要表图数值，C. 完整复现主文和 SI 的实验矩阵，还是 D. 先 A 后逐步逼近 B/C？
  2. 你更希望 YONOD 的创新点优先落在哪一类：A. 更严谨的评估协议，B. 新增/改进描述符或特征组合，C. 面向不平衡产率分布和高产率识别的分析，D. 自动化报告/可复现实验工具化，还是 E. 跨反应数据集迁移验证？
  3. 最终你最想拿什么来证明“这个复现对项目有价值”：A. 与论文相近的复现实验结果，B. YONOD 在某个数据集/划分上发现的新现象，C. 一套可复用的 benchmark pipeline，D. 答辩/论文可直接使用的图表和结论？
- **用户回答**
  1. D
  2. D
  3. C+D
- **提炼结论**
  - 复现目标采用分层策略：先获得核心工作流和趋势结论，再逐步逼近主要表图数值和完整实验矩阵。
  - YONOD 的优先创新点确定为“自动化报告/可复现实验工具化”，而不是优先发明新模型或新描述符。
  - 最终价值证明需要同时包含可复用 benchmark pipeline，以及答辩/论文可直接使用的图表和结论。
- **影响的目标文档章节**
  - 核心目标
  - 期望成果
  - 成功标准
  - 范围界定
  - 约束条件
  - 阶段规划
<!-- GOAL-QA-R002-END -->

### QA-R003：项目文件夹整理范围确认
<!-- GOAL-QA-R003-START -->
- **提问时间**：2026-08-25 12:08
- **提问目的**：用户希望对当前项目文件夹整理提出建议。本轮用于确认整理目标偏向“研究项目可读性”“代码工程规范”“轻量清理”还是“正式交付结构”，并避免误删或误移动仍有价值的中间文件。
- **问题列表**
  1. 这次文件夹整理更想服务于哪种场景：A. 日常开发更清爽，B. 大创/答辩交付更规范，C. 论文复现实验可追溯，D. 三者兼顾但先不大改结构？
  2. 对缓存、IDE 文件和临时验证脚本，你希望采用哪种策略：A. 只更新忽略规则并保留本地文件，B. 移到统一的本地临时区，C. 确认无用后删除，D. 暂时只给建议不实际操作？
  3. 对 `docs/参考文献`、`project-docs/`、`dataset/`、`WEIGHTS/` 这几类材料，你更希望整理成：A. 按“代码/数据/文献/实验结果/项目文档”分区，B. 按“VJETHBKM 复现流程”分区，C. 保持现状，仅补充命名和说明文档？
- **用户回答**
  1. D：三者兼顾但先不大改结构。
  2. C：缓存、IDE 文件和临时验证脚本在确认无用后删除。
  3. A：按“代码 / 数据 / 文献 / 实验结果 / 项目文档”分区。
- **提炼结论**
  - 文件夹整理目标应作为 YONOD benchmark 工具化的配套目标，而不是独立的大规模重构任务。
  - 整理方向需要兼顾日常开发、大创/答辩交付和论文复现实验追溯，但短期策略是轻量梳理、先说明边界、再逐步清理。
  - 用户倾向在确认无用后删除缓存、IDE 文件和临时验证脚本，因此删除动作必须建立在逐项用途确认、Git 跟踪状态确认和引用关系检查之后。
  - 目录分区采用“代码 / 数据 / 文献 / 实验结果 / 项目文档”的通用科研工程结构，优先利用现有 `yonod/`、`dataset/`、`docs/参考文献/`、`WEIGHTS/`、`project-docs/` 等目录职责。
  - `_verify/` 这类临时验证脚本不能直接视为垃圾文件，应先判断其是否记录历史修复过程、是否仍可用于回归验证，再决定归档或删除。
- **影响的目标文档章节**
  - 核心目标
  - 期望成果
  - 成功标准
  - 范围界定
  - 约束条件
  - 阶段规划
<!-- GOAL-QA-R003-END -->

### QA-R004：严谨模型比较架构的落地边界
<!-- GOAL-QA-R004-START -->
- **提问时间**：2026-09-01 14:04
- **提问目的**：用户已明确采纳统一切分、反应相似性分组重复交叉验证、逐折预测、统计比较、可恢复实验编排与 LightGBM 基线。本轮用于确定反应相似性分组的首选定义、第一阶段验证规模和统计结论的使用边界，避免后续目标在关键评估假设上含糊。
- **问题列表**
  1. 对“反应相似性分组”，你希望第一阶段优先按哪种方式定义：反应指纹相似性聚类、底物骨架/组分留出，还是两者均提供并作为可配置选项？
  2. 第一阶段是否将“5 次重复 × 5 折分组交叉验证”作为默认验收配置，并允许用更小的 smoke 配置先验证流程？
  3. 统计报告中，你希望主要比较的对象是“不同模型在同一描述符下的差异”“不同描述符在同一模型下的差异”，还是两类比较都要？
- **用户回答**
  1. 反应指纹相似性聚类与底物骨架/组分留出两种策略均可配置。
  2. 同意以 5 次重复 × 5 折分组交叉验证作为默认验收配置，并保留 smoke 配置。
  3. 模型比较与描述符比较两类统计报告均要覆盖。
- **提炼结论**
  - 用户已明确决定将统一可复用的数据切分计划、面向反应相似性的分组重复交叉验证、每折预测保存、指标与统计比较模块、可恢复实验编排和 LightGBM 基线纳入 YONOD。
  - 反应分组支持两类可配置策略；默认评估为 5×5 分组重复交叉验证并保留 smoke 配置；统计比较同时覆盖模型与描述符两条维度。
- **影响的目标文档章节**
  - 核心目标
  - 成功标准
  - 范围界定
  - 约束条件
  - 严谨模型比较架构目标
<!-- GOAL-QA-R004-END -->

### QA-R005：描述符预计算与建模解耦边界
<!-- GOAL-QA-R005-START -->
- **提问时间**：2026-09-04 12:49
- **提问目的**：用户希望把当前“描述符 × 模型”循环改为“先完整生成并持久化描述符，再统一读取描述符建模”的两阶段流程。本轮用于确认描述符集合、落盘契约和缓存复用规则，避免后续实现对“所有描述符”或“已经计算好”的理解不一致。
- **问题列表**
  1. “先计算所有描述符”是指本次任务配置中选中的全部描述符，还是无论配置如何都计算 YONOD 已注册的全部描述符？
  2. `descriptors/` 中希望采用哪种持久化粒度与格式：每个描述符一个独立文件，还是一个合并文件；文件是否需要同时保留 `sample_id`、有效性标记、特征列/维度和描述符配置元数据？
  3. 当目标结果目录已有描述符文件时，系统应如何处理：在数据与描述符配置一致时直接复用、变更时自动重算，还是由用户显式选择复用/覆盖；描述符阶段部分失败时是否应禁止进入建模阶段？
- **用户回答**
  1. “所有描述符”指本次配置中选中的全部描述符。
  2. `descriptors/` 采用每个描述符一个独立文件；文件必须保存 `sample_id`、有效性标记、特征列或特征结构、特征维度，以及描述符配置元数据。
  3. 数据与描述符配置一致时自动复用，发生变化时自动重算；单个描述符失败只跳过该描述符，并在最终报告中标注失败原因，不阻断其他描述符建模。
- **提炼结论**
  - “预计算全部描述符”的范围严格受本次运行配置控制，不额外计算未选中的昂贵描述符。
  - 流程正式分为“描述符预计算/复用”和“读取已落盘特征建模”两个阶段，建模阶段不能再依赖描述符计算入口。
  - 描述符文件是结果包的正式、长期保留产物，而不只是单进程生命周期内的临时缓存；每个描述符独立保存，并具备样本对齐、有效性和配置追溯信息。
  - 缓存是否可复用由输入数据与描述符配置共同决定；模型配置与交叉验证配置变化不应触发描述符重算。
  - 描述符失败采用局部隔离和显式报告策略，保证其余描述符仍可完成建模，同时避免失败被静默忽略。
- **影响的目标文档章节**
  - 核心目标
  - 期望成果
  - 成功标准
  - 范围界定
  - 约束条件
  - 严谨模型比较架构目标
<!-- GOAL-QA-R005-END -->

### QA-R006：MFP 与 OHE 的通用任务兼容范围
<!-- GOAL-QA-R006-START -->
- **提问时间**：2026-09-05 18:09
- **提问目的**：确认已完成论文协议实现的 MFP/OHE 是否应仅作为严格 benchmark 的特例，还是应进入普通任务的描述符/特征选择界面，并界定 OHE 的无泄漏前提。
- **问题列表**
  1. 是否希望普通 `main.py`、JSON 配置和 `yonod.py` 向导也能选择 MFP 与 OHE，而非只能通过论文 benchmark YAML 使用？
  2. 对 OHE，是否接受其虽出现在统一特征库中，但只能在每个交叉验证训练折 `fit`、再对训练/验证折 `transform`，不能生成全数据预计算矩阵？
- **用户回答**
  1. 希望将 MFP 和 one（结合上下文指 OHE）兼容到描述符库里，其他普通任务也可以使用。
  2. 结合既有结论，接受 OHE 采用折内拟合/转换以避免数据泄漏。
- **提炼结论**
  - MFP 与 OHE 的可用范围从论文严格 benchmark 扩展到常规反应预测入口；二者应在同一特征目录中可发现，但不能因统一展示而抹平生命周期差异。
  - MFP 可遵循静态描述符的预计算与复用契约；OHE 必须保留训练折拟合、验证折仅转换、按折保存状态与元数据的无泄漏契约。
- **影响的目标文档章节**
  - 核心目标
  - 期望成果
  - 成功标准
  - 范围界定
<!-- GOAL-QA-R006-END -->

<!-- GOAL-QA-LOG-END -->

## 备注
目标文档已根据六轮澄清收束为当前版：前两轮确定 `VJETHBKM` 复现与 YONOD benchmark 工具化方向，第三轮补充文件夹整理目标，第四轮确定严谨模型比较架构的分组、验证和统计范围，第五轮明确描述符预计算、持久化、复用和失败隔离契约，第六轮将 MFP/OHE 的可用范围扩展到普通任务入口并确认 OHE 的折内无泄漏边界。建议下一步调用 **project-planner-cn** agent，将本目标拆解为具体实施计划、数据产物 schema、任务顺序、兼容性改造和验证方案。
